# Java-Hibernate-ORM-Proof-of-Concept
A simple proof-of-concept project demonstrating Hibernate ORM capabilities. This POC performs CRUD (Create, Read, Update, Delete) operations using Hibernate with a relational database. Includes Hibernate configuration, entity mapping, SessionFactory usage, and transaction management.
